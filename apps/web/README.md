# Web Workbench

Placeholder for the future Next.js Operations Workbench.

The first MVP skeleton builds the backend simulator-backed workflow before adding
UI code, so this directory intentionally has no application implementation yet.

When the evidence timeline UI is implemented, use
`docs/EVIDENCE_TIMELINE_API_CONTRACT.md` as the API/UI contract for
`GET /sentinel/detections/{detection_id}/evidence`.
