# Drift Scenario Fixtures

The simulator creates deterministic scenario data at runtime. Snapshot fixtures can
be added here when a scenario becomes part of a regression test or public demo.

Current MVP scenarios:

- `normal`
- `gradual_drift`
- `sudden_excursion`

