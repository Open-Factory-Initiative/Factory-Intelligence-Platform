# Factory Events

Shared event contracts for service boundaries in the Factory Intelligence Platform.

The package validates the MVP event envelope and the first Process Sentinel event
payloads. Services should import these models instead of passing unvalidated
dictionaries across boundaries.

